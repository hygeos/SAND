[tool.poetry]
name = "sandd"
version = "0.1.0"
description = "SANDD - Satellite Acquisition and Data Downloader"
authors = ["HYGEOS"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.10"
numpy = "<2"
requests = "*"
eumdac = "*"
shapely = "*"
pyotp = "*"
core = {git = "https://github.com/hygeos/core.git"}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
