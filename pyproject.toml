[tool.poetry]
name = "sandd"
version = "1.0.0"
description = "SANDD - Satellite Acquisition and Data Downloader"
authors = ["Nathan Capon <nathan.capon@hygeos.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
eumdac = "^2.2.3"
shapely = "^2.0.5"
core = {git = "ssh://git@github.com/hygeos/core.git", develop = true}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
